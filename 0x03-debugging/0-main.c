#include "main.h"
/**
 * main -  test that positive_or_negative() gives the correct output
 *
 * Return: Always 0 (Success)
 *
 */

int main(void)
{
	int i;

	i = 0;

	positive_or_negative(i);
	return (0);
}

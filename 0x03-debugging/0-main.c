#include "main.h"
/**
 * main -  test that positive_or_negative() gives the correct output
 *
 * Return: Always 0 (Success)
 *
 */

int main(void)
{
	int n;

	n = 0;
	printf(positive_or_negative(n));
	return (0);
}

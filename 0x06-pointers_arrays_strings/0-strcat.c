#include "main.h"
#include <string>
/**
 * main - concatenates two strings.
 * @dest: destination
 * @src: source
 * Return: Always 0.
 */
char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
